# Templates for flakes / dev environments
This directory holds template flakes to create dev environments.

## Usage dev environment flakes
```bash
cp <template>.nix <project path>/flake.nix
(in project dir)
nix develop
```
